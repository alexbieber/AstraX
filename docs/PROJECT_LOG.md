# Codex-X 项目维护日志

本文件记录面向维护者的根因、设计决策、验证依据和遗留风险。它与
`CHANGELOG.md` 分工如下：

- `CHANGELOG.md` 面向用户，记录每个发布版本可感知的新增、调整和修复。
- 本文件面向开发维护，解释为什么修改、哪些约束不能破坏，以及如何验证。

日志不得包含 Token、认证文件内容、用户数据或仅适用于某台电脑的隐私路径。

## 2026-09-18：Windows 发布验证修正

- v0.3.19 草稿构建在 Windows 测试阶段发现路由重启恢复问题，未公开发布该版本。
  数据库存储的目录比较键会统一斜线和大小写，启动恢复却直接将其用作原生路径和运行实例键；
  Windows 的 verbatim 前缀及规范大小写因此与交互入口不一致。恢复时现先还原原生分隔符，
  再使用交互入口相同的路径解析，新增持久化目录与运行实例一致性的回归。
- 流式停止测试在上游等待期间同步探测已关闭端口，Windows 的默认连接等待耗尽了测试的静默期限；
  改为有明确短期限的连接探测，仍验证停止监听不截断已经开始的响应，不放宽运行时超时策略。
- 修正沿用新的 v0.3.20 标签重新发布，保留失败构建记录，不覆盖 v0.3.19 标签。

## 2026-09-18：按 CC Switch 对齐路由控制与故障转移

- 对照 CC Switch `06082e189d65e6d6dbadc35dacdac1ce6c79d89a` 的 UI → IPC → 数据库 → 运行时链路，
  将监听服务、Codex 请求接管、自动故障转移拆成三个独立状态；设置版本升级为 2，并迁移旧单开关与
  “主供应商之外的候补”记录。队列现在保存完整 P1 起始顺序，单项队列可用，不再要求模型 ID 相同。
  保存设置、首次开启自动模式时先启用有效 P1；空队列只在首次开启时自动补入当前已保存的第三方供应商。
- 参数采用 Codex 对应默认值与边界：重试 3 次、响应头/流式首块分别等待 60 秒、流式静默 120 秒、
  非流式单次总期限 600 秒；熔断失败/恢复阈值为 4/2，等待 60 秒，错误率 60%、最少 10 个样本。
  前后端都校验；支持 IPv4/IPv6/localhost，端口或地址变更要求停止监听。通配监听写给本机客户端时
  使用回环地址，绑定非回环接口也保留本地认证。
- 按上游状态机适配 Closed/Open/HalfOpen 熔断与单探测许可；参数热更新不清空未改变供应商的健康记录。
  尝试上限为重试次数加一，每家每请求至多一次，熔断跳过不占次数。已提交响应后不重放整条请求；
  含服务端状态的请求只尝试本次快照队首，不提供历史 response/session 的永久供应商绑定。
- 官方账号支持独立原生 HTTP/SSE 接管，始终排除第三方自动队列。Codex 负责登录和 token 刷新；
  每请求验证当前选中 profile、实时 token/account，OAuth 与官方 API Key 分别固定官方上游，不从
  未选中账号借用认证。官方模型端点只提供受管本地目录或空目录，不宣称完整远程模型发现。
- 手动切换或编辑当前供应商使用完整操作包装，暂时撤下覆盖后再恢复接管，不再直接关闭全部路由。
  自动成功选择按监听实例身份、运行 revision、令牌和配置代次验证后更新逻辑当前项；过期返回不能覆盖
  后来操作，也不能在停止重启后因 token/revision 恰好相同而污染新实例。
  路由设置、供应商更新、退出及崩溃恢复继续保留条件写入、认证写入顺序和失败回滚，恢复成功后才停监听。
- 官方状态、额度、登录监控和快照读取解包后的逻辑配置；本地地址、认证令牌和代次头不能变成保存的
  供应商凭据。路由事件只提示前端静默重读当前目录，使用目录/请求代次检查并延后编辑期间的更新，
  不清空页面和未保存草稿。既有通用配置继承、官方/第三方认证隔离及内部会话过滤修复继续保留。
- 本轮替代 2026-09-17 初版中的同模型限制、单开关、固定 30 秒冷却和手动切换关闭功能等约束；
  不包含 CC Switch 的全部协议转换器、其他应用代理、日志计费或 OAuth 设备码反代。
  具体对应、适用边界和上游差异见 [路由对照说明](ROUTING_CC_SWITCH_PARITY.md)。
- 复用的熔断器与压缩处理保留 MIT 来源和版权；`THIRD_PARTY_NOTICES.md` 记录上游提交及完整许可。
  验证使用合成凭据、临时 CODEX_HOME 和本机服务，没有对用户真实供应商发送测试请求。
- 598 项 Rust 测试、51 项前端测试、类型/格式/diff 检查通过，macOS `.app` 构建成功。
  浏览器验证三层开关、P1 排序、不同模型队列、参数范围与撤销、静默超时设为 0、72.5% 错误率、
  熔断重置/恢复状态、暗色对比，以及失败保存后的草稿保留。
  解锁后已正常退出旧版、替换并启动本地 App；新旧 ZIP 的完整性、可执行文件哈希与签名均通过校验。
  实际安装版确认路由页面、已有队列和参数可读取，独立监听能启动并停止，结束后恢复原有关闭状态。
  全程 config.toml 与 auth.json 哈希一致，没有接管真实账号或发送供应商测试请求。
  应用目录仅保留一份 Codex-X，旧版与构建副本压缩归档；尚未推送 GitHub。

## 2026-09-18：第三方供应商通用配置继承

- 已只读确认：当前 live 文件已退化为供应商/模型及少量根字段，默认官方账号的同目录可信快照仍保留
  MCP、desktop、marketplaces 等完整集成配置。因此官方新增能显示完整内容，第三方只能继承精简 live。
  另有启用漏洞：旧实现将 reasoning/disable_response_storage 等任意额外根字段视为完整配置，
  导致带这些字段的旧模板可整体覆盖当前通用设置。
- 新增只读配置底稿接口，添加入口先读取一次，所有自定义/厂商预设共用该底稿；读取失败直接提示，
  不继续用精简模板替代。目录/请求变化使旧返回失效。旧精简模板编辑时也补入当前通用设置。
- 当前文件仅有供应商相关字段时，可从同目录默认官方快照恢复 mcp_servers、desktop、marketplaces、
  plugins、projects 五个集成表；不复制历史登录凭据、供应商路由、模型、安全策略、计费档位或通知命令。
  正常完整 live 为通用设置的权威来源，不用历史快照覆盖它；底稿生成本身不写 live 或 auth.json。
- 普通启用/编辑供应商基于最新通用配置，只更新供应商与模型设置；旧模板缺项不会删除 MCP 等设置，
  历史模板的过期通用值也不会覆盖现值。手工编辑当前供应商完整 TOML 有独立显式应用路径，保留用户
  增删通用设置的能力。1M 复选框不被视作手工修改通用配置。
- 新建或换接口地址时清理上一个供应商的专属请求头、环境认证和查询参数，MCP 自己的 env 原样保留。
- 每个 CODEX_HOME 在首次成功应用修复后的配置时记录处理标记，之后不再把用户有意清空的通用配置
  当成旧版损坏反复回补；预览和仅保存未启用的供应商不置标记，失败应用也不置标记。
  标记写入失败沿用现有 config/auth/官方快照回滚，不留下半完成的设置。
- 往返回归另外复现了普通切回官方会重放旧配置的问题，官方切换现也保留最新通用设置，仅从目标官方
  配置读取模型、官方路由与账号约束；账号认证切换顺序和显式官方编辑语义保持原有规则。
- 553 项后端测试、45 项前端测试、类型/格式检查与 macOS 构建通过。新增回归覆盖旧精简模板、已有
  精简 live、跨目录快照拒绝、MCP env、未知通用字段、显式删除、失败回滚、模型上下文与账号往返隔离。
  模拟界面确认自定义/千问/DeepSeek 与官方预设切换、保存后再编辑都保留集成表；读取底稿失败停留列表
  并提示错误，恢复读取后可正常新增。
- 本地 App 已重新构建、签名/二进制校验、替换并启动；真实目录下只读打开自定义及千问新增草稿，
  确认五个集成表与 node_repl.env 都存在。config.toml、auth.json 和官方快照前后哈希一致，
  没有保存/启用测试供应商。旧 App 与构建副本已压缩归档，仅保留一份安装；尚未推送 GitHub。

## 2026-09-17：回退会话文件名的 ID 校验回归

- 上次内部类型检查增加了文件名与首条 session_meta.id 的核对，但直接取文件名末尾 UUID，
  把 thread/revert 生成的 `rollout-<timestamp>-<thread-id>_<rollout-id>.jsonl` 中 rollout ID
  当成线程 ID。实际首条元数据和活动 SQLite 都仍使用前面的稳定 thread ID，导致正常记录误报。
  已只读核对用户报告的三个文件的身份字段与索引，不读取输出正文、不改动实际日志或数据库。
- 依据 [Codex 0.154.0 文件名定义](https://github.com/openai/codex/blob/rust-v0.154.0/codex-rs/rollout/src/rollout_file_name.rs)，
  文件身份与查找匹配统一解析稳定 thread ID；支持普通与回退文件名，并保留现有压缩文件名匹配。
  rollout ID 不作为会话别名，不能据此选中另一条会话。索引权威路径、元数据精确匹配和内部类型检查保留。
- 新增四项回归，覆盖普通/回退文件名、日志 ID 不冒充会话 ID、真正元数据不一致、内部回退记录不修改、
  从检查到事务同步成功，以及 SQLite 以错误 ID 引用文件时仍拒绝同步。
- 544 项后端测试、格式检查和 macOS 构建通过；本地 App 已校验替换并启动，旧安装和构建副本压缩归档。
  本次未推送 GitHub，未对真实会话执行同步、删除或重命名。
  更新后只读复查实际目录，界面显示「普通会话已同步」，上述 ID 不一致及异常文件警告已消失。

## 2026-09-17：可选的供应商故障自动切换（#49）

- 按用户要求只在设置新增第三个标签「故障自动切换」，沿用标签过渡；供应商列表不增加入口。
  默认关闭，当前已启用的第三方供应商优先，最多选择 8 家备用并调整顺序。运行状态轮询保留草稿，
  切页暂停、目录变化或旧响应不能覆盖新页面；开启/关闭提示重新打开 Codex。
- 对照 CC Switch 06082e189d65e6d6dbadc35dacdac1ce6c79d89a 的代理与熔断实现：故障必须由实际请求判断，
  不能用模型列表测速代替。新增仅监听 127.0.0.1 的 HTTP Responses 转发，接管当前 provider 的传输字段，
  不修改模型、模型目录、会话、auth.json 或全局认证；官方登录不参与候补。
- 每次请求只尝试显式选中的候补，且其配置必须包含完全相同的模型 ID。连接/超时、认证/限流及部分
  服务端故障触发切换；普通参数错误不轮询。失败供应商冷却 30 秒，每家每次请求最多尝试一次。
  服务端会话状态绑定的请求不跨供应商重放；已发送响应后绝不重试整条请求，避免重复生成或工具调用。
- 入站要求随机本地令牌、固定 Host，拒绝浏览器来源及任意目标转发；请求头、正文、并发与等待均有上限。
  出站独立使用每家保存的凭据，不透传本地令牌或官方账号头，不跟随重定向；不记录请求正文和密钥。
- 应用数据库增加按 CODEX_HOME 隔离的设置与恢复记录，先备份/写恢复记录再原子覆盖传输字段。
  关闭功能、手动切换/编辑当前供应商及退出时先恢复直连再停监听；恢复失败阻止退出并保持服务可用。
  成功退出后拒绝排队的重新启用请求，避免遗留失效地址。启动按原端口恢复，无法恢复则回到直连并提示。
- 仅还原仍属于本功能的字段，保留随后编辑的其他设置；历史恢复记录也可识别旧备份里的本地地址。
  状态、供应商识别、编辑草稿及旧 TOML 提交都使用真实上游，防止产生 localhost 供应商或保存本地令牌。
  保存/删除/导入供应商会即时刷新运行队列，已失效的候补使自动切换暂停；后台也检查外部配置变化。
- 验证：540 项 Rust 测试和 45 项前端测试、类型/格式检查通过；其中 21 项真实本机 HTTP/SSE 测试
  覆盖故障候补、流式首块边界、凭据隔离、关闭/改队列、认证与长度限制，以及持续流超过单次读取超时。
  浏览器确认明暗主题、三个标签切换、添加/排序/保存、失败保留旧状态、冷却显示和离开标签停止轮询。
  本次仅使用合成服务与临时配置验证，没有启用用户真实供应商的自动切换，也没有向其发送测试请求。
- 本地 macOS App 已构建、签名/二进制校验、替换并启动；旧安装与构建副本保留为校验过的 ZIP，
  系统只注册 Applications 中的一份 App。尚未推送 GitHub，也未发布 Windows 安装包。

## 2026-09-17：内部会话误报待同步与侧栏污染

- 原过滤主要识别 subagent，遗漏了 internal/guardian、memory_consolidation 及其 SQLite 表示；
  泛化的 thread_source=user 还会清除已有的内部来源或 spawn 关系。跳过 rollout 后，部分内部 ID
  仍留在数据库同步候选中，可能被补入桌面会话目录。这些类型在 Codex 0.153.4 已存在，并非只由新版引入。
- 活动 SQLite、其权威 rollout 的首条 session_meta、spawn 关系和已有桌面目录共同提供分类依据；
  当前存储中的明确内部证据不会被默认 user 标记覆盖。旧数据库的同 ID 副本不覆盖活动记录的归属。
  不按标题、模型名、空正文或缺少用户事件猜测；普通父会话及仅有 forked_from_id 的用户分叉继续保留。
- 检查计数、预览、rollout/SQLite 写入和目录补建均排除内部任务；事务提交前后复核 rollout 身份，
  分类发生变化时回滚。内部记录不会因同步改写 provider、工作目录或缺失标记，也不新增侧栏条目。
  列表达到上限时优先保留普通会话；“显示内部会话”仅影响展示，不会把内部记录加入同步候选。
- 用量扫描共用内部类型识别，内部用量仍计入总量，有明确父会话时合并，不生成单独的用户会话行。
  相同 ID 的多个日志仍按原规则合并用量，但类型和子代理父关系以当前数据库引用的文件为准，
  避免旧内部副本隐藏当前用户会话；仅有首条 thread_source 标记的内部任务也按相同规则处理。
- 回归使用临时数据库与日志，覆盖自动审核、来源冲突、真实用户分叉、旧副本、超长元数据与写入竞态。
  本次不对真实会话执行同步或清理；既有侧栏污染不会自动删除，防止误删用户历史。
- 505 项 Rust 测试、45 项前端测试、类型及格式检查通过。模拟界面确认默认仅显示普通会话，
  开启内部会话显示后仍无待同步标记，且仅有内部差异时同步按钮不可用；尚未在 Windows 真机验证。
- macOS App 已重新构建、签名与二进制校验、替换并启动；旧安装和构建副本经校验后压缩归档，
  仅保留一份应用注册。本地版本仍为 0.3.18，修复列在 Unreleased，尚未提交或推送 GitHub。

## 2026-09-17：v0.3.18 发布准备

- 汇总后台配置检查、统一检查入口、会话/MCP/Skills 导出导入、大日志统计和模型推理等级/DeepSeek WS 修复。
  发布说明沿用四条面向用户的更新，根目录、桌面、Tauri、Cargo 清单及锁文件统一为 0.3.18。
- 发布前沿用已完成的 490 项后端、45 项前端回归与本地 App 验证，版本更改后复核类型、构建和锁文件。
  本次停止继续调查 Codex 前端临时显示 Luna 的问题，不改历史会话或官方模型偏好。
- 官网源码和本地测试截图继续留在本机，未纳入发布提交；由版本标签触发现有四平台构建、签名与发布校验。

## 2026-09-17：模型推理等级与第三方 WebSocket 回退

- 所有第三方模型映射统一生成完整八档菜单（none/minimal/low/medium/high/xhigh/max/ultra），默认 high。
  用户启用供应商后直接在 Codex 中选择，无需在 Codex-X 逐模型配置，不增加档位选择表单或厂商映射说明。
  菜单可选不代表所有上游都实现了八种独立强度，实际效果仍由接口和 Codex 决定。已有模型记录无需增字段。
  目录按内容生成新文件，不覆盖旧目录或模型缓存。
- 自定义目录补齐 supported_reasoning_levels 和 default_reasoning_level；有档位时为旧 Codex 保留
  supports_reasoning_summaries=true，同时关闭摘要参数、默认摘要设 none。Codex 0.153/0.154 已将
  effort 独立发送；ultra 在客户端可能折算成 max，并不保证独立的上游档位。
  参考 CC Switch 06082e1 的 CodexFormFields/codex_config 实现及 DeepSeek 官方 Responses/Codex 文档，
  没有复制上游代码、缓存提示词或伪造 GPT 的其他能力。
- WSS 405 根因是第三方草稿继承了官方 custom 表中的 supports_websockets=true。传输能力现在归属
  对应端点：新建（包括携带同 URL 的继承模板）默认 false；编辑自身同端点模板保留明确值，换地址重置。
  api.deepseek.com 精确匹配禁用 WS，独立代理域名不干预；官方登录仍为 true，不改全局 WS feature。
  已保存 DeepSeek 重新启用也会修正，当前配置通过配置检查提示并由用户点击后备份修复。
- 验证覆盖完整档位元数据与默认值、旧记录、供应商官方/第三方切换、同端点新建与编辑区分，
  以及现有错误配置的只读检查和可选修复。模型目录刷新需要重新启用供应商并重新打开 Codex。
  最终 490 项后端测试、45 项前端测试、类型检查与 macOS 构建通过；本地 App 已备份、校验并替换启动，
  只保留一份应用注册。未使用用户密钥请求上游，也未重启用户的 Codex 客户端。尚未推送 GitHub。

## 2026-09-12：会话导出、Skills/MCP 归档与大日志统计

- 会话列表新增单条 Markdown 导出和多选 ZIP 导出，只包含明确选择的会话。复用活动 SQLite 与权威
  rollout 路径，验证存储根、普通文件和 session_meta.id，支持非标准文件名；正文按角色与事件类型筛选，
  过滤系统/工具/推理记录，以计数方式消除 event_msg 与 response_item 镜像，同时保留重复对话轮次。
  图片/音频使用占位，记录按行选择解析。单条记录仍有 64 MiB 保护上限，损坏或过大记录会报告部分导出。
- Skills/MCP 使用带版本 manifest 的 ZIP，保留配置、文件权限、空目录、启用状态和备注；当前标签页可导出
  全部内容，同一 ZIP 入口识别普通 Skill 包与 Codex-X 归档。本地 ZIP 取消压缩与展开后的 20 MiB 大小上限，
  使用 rfd 原生文件窗口和 Rust 文件流，避免大文件在前端 ArrayBuffer/整数数组及 IPC 中多次复制。
  本地归档可能包含 MCP 连接凭据，导出入口有保管提示；不上传归档或会话。
- ZIP 路径、重复路径、链接与特殊文件校验保留；导入预先检查所有同名冲突，不覆盖不同内容。
  MCP 配置与数据库变更共同回滚，失败移除本次创建的 Skill；导出临时文件使用受限权限、目标快照校验
  和原子替换，禁止写回应用数据目录。用户取消文件选择时不执行读写。
- 导入已有的假 stdio 项来自无效/空 MCP 配置的默认展示；候选现在必须具备有效 command 或 URL，
  预览与导入复用同一验证与去重路径，已存在的实时配置不再计入新增。兼容包装 JSON，正确处理
  config.toml 中 enabled=false 的条目。暗色弹窗补齐统计格子、列表文字、边框及警告区域的颜色。
- 用量扫描将 2 MiB 整行跳过改为 64 KiB 缓冲快路径与超长行流式反序列化，跳过大正文/图片字段，
  仍读取 token_count、模型与父子会话信息；保留增量偏移、扫描预算、坏行恢复和尾行补写规则。
- 验证：479 项 Rust 测试、45 项前端测试、类型检查和 macOS 构建通过，包含 >20 MiB ZIP、归档往返、
  冲突与错误回滚、导出正文去重、非常规会话路径，以及 >2 MiB 图片/工具/计费事件等回归。
  浏览器确认暗色导入弹窗、无新增提示、分标签导出、单/多会话导出与取消操作；本机确认原生打开/保存
  窗口可正常取消，原先的四个已有 MCP 不再被提示重新导入。本地 App 已替换，旧 App 和构建副本均经
  ZIP/二进制校验后归档，仅保留一份应用注册。尚未推送 GitHub。

## 2026-09-10：后台配置检查与可选修复

- 后续界面调整：将设置中的首次向导入口与配置检查卡片合并为同高的一行「环境与配置检查」。
  行内仅保留配置状态和检查按钮；环境详情、配置问题和修复方案统一放进原检查窗口。
  首次安装自动打开，普通手动检查不删除或重置首次使用标记；后台静默检查保持独立。
  环境检测加载或失败时窗口仍可打开，配置检查/修复继续可用；修改目录后需重新检查再提供修复，
  避免把旧目录结果误用于新目录。查看过的问题不再紧接着重复弹出后台提醒。
- 新增独立的只读检查 IPC；窗口可见且未编辑/写入配置时后台检查，回到窗口时唤醒。
  不依赖 get_codex_state 成功，也不占用供应商操作的 loading 状态。稳定错误需间隔至少 1 秒确认两次。
  同一目录的同一问题保存提醒记录；30 秒提醒支持直接修复和进入通用设置，忽略后提示设置入口。
  弹窗、隐藏窗口和其他通知期间暂停提醒计时；无关配置编辑更新修复指纹但不缩短剩余时间。
  临时缺失/空文件不会直接清除提醒记录，正常配置也经稳定确认后才允许以后再次提醒。
- 检查 TOML 语法和供应商表、名称、选择标识、认证开关及协议字段，保留 Codex 的合法默认值。
  依据 [Codex 配置说明](https://learn.chatgpt.com/docs/config-file/config-reference) 和
  [rust-v0.153.4 配置校验](https://github.com/openai/codex/blob/rust-v0.153.4/codex-rs/config/src/config_toml.rs)：
  name 对普通自定义供应商必需，省略 base_url、wire_api、认证/WebSocket 开关本身不是错误。
  不把 Chat Completions 协议盲改为 Responses；无法确认的问题提供查看配置入口。
- 旧会话恢复可以使用数据库中的 model_provider，所以根配置正确时也要检查历史引用。
  只查询活动 SQLite 中最近 10,000 条记录的不同标识，不读取会话正文；有效的顶层 profile 配置可解释
  相应引用，避免误报。仅当缺失一个引用且有唯一完整来源时提供补齐方案，提示明确说明沿用哪个供应商。
  多个来源或无法确定时不猜测，不改数据库、auth、模型或其他配置文件。
- 修复只在用户点击后执行：备份、文件锁、配置/认证判断/会话引用/profile 指纹复核及原子写入。
  外部修改使旧方案失效；符号链接配置不覆盖。错误提示不包含源文件行或凭据。
  这是供应商相关的有限检查，不宣称完整验证 Codex 所有扩展设置。
- 验证：445 项 Rust 测试、45 项前端测试、类型检查及构建通过。浏览器验证首页读取失败仍可进入设置修复，
  提醒到期后供应商可操作，额度弹窗期间提醒隐藏，关闭提醒后的设置提示可见，手动问题无自动修复按钮。
  本地 macOS App 已构建、签名校验、替换并启动；旧安装与构建副本均经完整 ZIP/二进制校验后归档，
  Launch Services 只注册 Applications 内的一份安装。此次尚未发布到 GitHub。

## 2026-09-09：v0.3.17 发布准备

- 将已完成本地验证的官方账号额度/重置次数、套餐徽标、主流预设、模型映射与供应商编辑修复整理为 v0.3.17。
  更新根目录与桌面 package.json、Tauri 配置、Cargo 清单及锁文件，发布说明按用户功能合并为四条。
- 发布前版本一致性、前端类型检查与构建、cargo check --locked 通过；沿用已通过的 415 项后端、30 项前端测试。
  现有 Release workflow 从版本标签构建四个平台，完成安装包、签名和更新清单校验后再发布。

## 2026-09-09：重置次数查询与套餐徽标

- 额度弹窗新增独立只读重置卡请求，读取 rate-limit-reset-credits 的 available_count，仅展示可用次数。
  与 usage 请求同时发起，各自返回即更新，独立处理加载与错误；空字段和失败不当作零，刷新失败可保留
  对应上次结果并明示。关闭、重开、切换账号或目录后丢弃旧返回，不提供兑换操作。
- 原生请求共用敏感请求头、固定 HTTPS 地址、超时、响应大小限制和凭据复核。文件锁采用非阻塞方式，
  因此仅在读取/复核凭据时用短互斥协调两请求，网络阶段不持锁，后端并发回归验证两路可重叠执行。
- 官方摘要从自己拥有的认证解析套餐，与邮箱共用有界 JWT 读取，刷新及注销遵循同一归属规则。
  数据字段对照 [Codex token_data.rs](https://raw.githubusercontent.com/openai/codex/main/codex-rs/login/src/token_data.rs)，
  展示映射按用户要求区分 Pro 20x / Pro 5x；未知套餐中性显示，套餐徽标不作权限或认证依据。
- 邮箱改为外层对齐容器、内层随文字收缩的边框，字号由 10px 增至 12px，后接套餐徽标。
  Pro 20x 使用金色，Pro 5x 使用紫色，Plus/Team 等使用各自配色；列表邮箱列不受“当前使用”挤动。
- 验证：415 项后端测试、30 项前端测试及类型检查通过。浏览器确认两接口同时待返回，重置次数先
  显示时额度仍可独立加载；在 App 最小窗口下，邮箱边框贴合内容、字号为 12px，套餐并列且操作按钮不换行。
- 本地 App 已构建、签名校验、替换并启动；旧版本和构建副本均已校验压缩归档，只保留一份安装。

## 2026-09-09：添加页文案与官方邮箱对齐

- 删除预设卡片上方可见标题与说明，保留 fieldset 的无障碍名称；API Key 输入提示改为普通用户可理解的文案。
- 官方列表邮箱移到独立居中的固定宽度列，左右信息与操作区域平均分配剩余空间，
  “当前使用”出现或消失不再改变邮箱位置。窄窗口保留单独换行布局，额度弹窗徽标不受影响。
- 类型检查通过；浏览器检查预设标题/说明已消失、API Key 提示正确。在 App 最小窗口下，两个官方
  账号轮流启用时邮箱左边界和宽度均一致，所有操作按钮仍在同一行，浅色与深色显示正常。
- 已构建、校验签名并替换启动本地 App。旧版本及构建副本均压缩归档，只保留 Applications 中的一份安装。

## 2026-09-09：预设继承与紧凑选择体验

- 添加流程内的官方/第三方表单共用稳定的 providers:add 过渡标识，切换卡片时直接更新内容，
  卡片自身颜色过渡也取消；从列表进入添加或编辑等正常页面导航仍沿用已有动画。
- 新增自定义和预设共用 newCustomProviderForm 取得当前完整 TOML。预设只提供自己的供应商字段及
  模型映射，再交给已有 build_provider_toml_draft 更新对应字段，保留 MCP、项目、推理及其他扩展设置。
  修复了此前用简版 TOML 作为预设草稿、使编辑页与保存模板缺失继承设置的问题。
- 卡片删除模型与品牌介绍，仅展示图标、名称和选择状态，高度从 70px 缩为 48px。
- 验证：20 项前端测试和类型检查通过，现有 Rust 完整 TOML 草稿回归通过；浏览器验证从自定义切到
  预设时 MCP/项目/推理设置保留，官方与第三方互切均保持页面过渡 idle 状态。
  保存并重新编辑后继承设置仍保留。本地 App 已构建、签名校验、替换并启动，旧 App 及构建产物压缩归档。

## 2026-09-09：主流厂商预设与统计切换体验

- 添加供应商的类型下拉替换为 8 张卡片：自定义、官方登录、DeepSeek、MiniMax、MiMo、Kimi、GLM、Qwen。
  MiniMax 国内/国际、MiMo 按量/Token Plan 以厂商内部按钮区分，预填各自 Responses 地址及真实模型映射。
  更换厂商、区域或套餐创建独立草稿并清空 API key，避免把上一服务凭据带到新地址；保存后仍走原有供应商流程。
  厂商品牌使用 Lobe Icons 的小型 MIT SVG 静态资产，来源、固定版本和许可随资产保留，无新增运行时依赖。
- PR #32 仅参考 MiniMax 预设选择思路，未合并其 Anthropic 配置。各预设按厂商当前官方文档核验，
  sources 记录在 providerPresets.ts；Kimi 只列原生 Responses 支持的 kimi-k3，GLM 使用 /api/v1，
  MiniMax 国内使用新版 api.minimax.cn。Qwen 上下文数值未能核准，因此保持可选默认值而不猜测。
  模型可用性取决于用户实际账号和套餐；本次不使用用户密钥向这些厂商发起测试请求。
- 用量页此前按完整筛选 key 获取 data，日期变化立即得到 null，导致图表卸载并替换为大片加载占位。
  改为保留同一 Codex 目录的已显示结果，轻量状态行明确待加载范围及当前展示范围，成功后原位更新。
  请求序号保护快速切换和错误返回，切换目录隔离结果，离开标签页取消旧请求的更新权。
  原有页面与设置标签页渐入渐出动画不变。
- 验证：19 项前端测试和类型检查通过。浏览器验证区域地址/密钥清空、模型选择、预设保存后映射保留，
  延迟并逆序完成统计请求时图表持续可见，旧响应不覆盖最后选中的范围；检查浅/深色及 App 最小窗口布局。
- 本地 macOS App 已构建、签名校验、替换并启动，旧安装和构建 App 压缩归档，只注册一份安装。

## 2026-09-09：供应商身份修复与第三方模型菜单

- 实时供应商匹配此前把模型计入身份，Codex 改变当前模型后会丢失保存项选择，编辑时误报 ID 冲突，
  切换时还可能把实时配置另存为新记录。现在先验证已选 ID 的接口和凭据，再恢复唯一匹配项；
  同接口不同密钥保持隔离，多个候选且无已选 ID 时不猜测，不覆盖或新增重复项。
- 新增模型映射编辑，支持当前模型、已获取模型批量导入，以及自定义菜单名称、实际模型 ID 和上下文大小。
  数据库迁移至 schema 5，映射随供应商保存、复制、导入更新和失败回滚保留。
  启用时通过 Codex 的 model_catalog_json 加载实际模型目录，不改 models_cache.json。
  显示名称不影响发给供应商的模型 ID；模型能力采用保守默认值，未承诺图片、托管工具或推理参数支持。
- 目录生成放在旧版简略模板与当前配置合并之后，避免丢失公共设置或重新带入旧菜单。
  切换无映射供应商、官方账号时移除 App 管理的目录指针，保留用户自己的目录文件。
  生成文件不可变，失败保留原配置和目录指针；保存记录与当前配置共同回滚。
  来源标记与内容摘要支持识别其他 CODEX_HOME 中生成的目录，避免跨目录切换残留菜单。
- requires_openai_auth=false 时 Codex 不从 auth.json 读取 API key。现在为当前接口写入显式 Bearer，
  清理冲突认证字段及 Authorization 头，其他请求头保留；供应商保存模板不包含明文 Bearer。
  requires_openai_auth=true 保持原 API key 认证路径。
- 参考 CC Switch 的原生 Responses 模型映射方式和 Codex 的外部模型目录结构。
  DeepSeek 当前已提供原生 Responses 接口，本次不增加 Chat Completions/Anthropic 协议代理。
  参考：[DeepSeek Responses](https://api-docs.deepseek.com/guides/responses_api/)、
  [DeepSeek Codex 配置](https://api-docs.deepseek.com/quick_start/agent_integrations/codex)、
  [Codex 配置参考](https://learn.chatgpt.com/docs/config-file/config-reference)。
- 验证中实际 Codex 0.153.4 的 model/list 只显示生成的第三方目录；本机模拟 Responses 服务收到正确的
  模型 ID、合成认证和 stream 请求，Codex 接收固定回复并完成 turn。DeepSeek 实际 Responses 请求返回 200。
  测试不把认证写入日志，不修改真实 Codex 配置；新菜单需要重启 Codex，已有会话保留原模型选择。
- 最终验证：409 项 Rust 测试、11 项前端测试和类型检查通过。浏览器验证批量导入、重复模型与非法
  上下文阻止保存、改名与映射重新编辑、启用提示及浅色/深色显示。
- macOS 本地 App 已构建、签名验证并替换启动；旧版本和构建产物已校验 ZIP 归档，
  LaunchServices 只登记 Applications 中的一份 App。本批修改未推送或发布。

## 2026-09-09：简化默认官方配置操作

- 移除列表与编辑页的“读取已保存配置”按钮、前端回调，以及专用的 restore_official_provider 和
  get_official_config_draft 旧 IPC 入口。前者原本用于手动恢复默认快照，并非日常账号切换。
  正常编辑加载、切换、独立快照保存与默认配置重置继续使用已有路径。
- 官方与第三方行的操作位置保持一致。默认官方行显示灰色禁用的删除按钮，悬停说明不能删除；
  原后端 DEFAULT_OFFICIAL_PROFILE_ID 删除保护继续生效。
- 清除不再使用的默认手动恢复实现，并保留对重置默认配置时保护命名账号的回归验证。
- 类型检查、Rust 开发检查及 15 项官方配置回归通过；macOS App 已构建、严格校验签名并替换启动。
  旧安装和构建产物保持 ZIP 归档，只登记 Applications 中的一份 App。

## 2026-09-09：登录自动识别、额度行布局与本地安装清理

- 登录状态过去只在启动或手动操作后读取。新增可见概览/供应商列表的 2 秒只读元数据检测，进入页面、
  窗口 focus 和恢复可见时立即检查；不读取完整 CodexState、不查询网络额度、不更新编辑草稿或全局 loading。
- 检测器单飞执行，焦点事件合并为一次后续检查；发送和接收时检查操作忙状态及共享请求序号，旧目录、
  已停止检测器与过期操作结果均丢弃。仅数据改变时更新列表并使更早的列表读取失效，失败静默重试。
  概览认证展示由当前官方摘要同步派生，保留原认证正文与配置状态。
- 卡片与额度弹窗共用 OfficialAccountBadge。额度窗口按实际时长排序并纵向排列，只显示接口返回的窗口。
  打开的额度弹窗在对应邮箱变化时关闭，避免展示另一个账号的旧额度，也不自动发起新额度请求。
- 本地旧 App 备份与构建产物也会被 LaunchServices/Spotlight 注册。今后本地安装保留压缩归档，验证
  ZIP 校验和及可执行文件摘要后移除多余 `.app` 实体并注销其注册，只保留 Applications 内的安装。
- 验证：11 项前端测试通过，其中 6 项检测器回归覆盖外部登录/注销、焦点合并、操作竞态、隐藏暂停与停止。
  浏览器模拟启用空账号后外部登录，邮箱及额度按钮约 2 秒更新，get_codex_state 调用数不增加、额度请求数为零；
  换邮箱后旧额度自动关闭。双窗口纵向定位及单周套餐检查通过，类型检查和 renderer 构建通过。
- macOS App 已构建、严格校验签名并替换启动；旧备份和构建 App 均改为已校验 ZIP 归档。
  最终 LaunchServices 只登记 `/Applications/Codex-X.app`，备份及构建目录没有重复 `.app`。

## 2026-09-09：官方账号徽标与订阅额度

- 官方卡片在名称/URL 块右侧、操作列左侧显示邮箱或登录徽标。当前账号展示字段只来自实时认证，
  非当前账号只来自自身显式快照；已注销的当前账号不会因旧快照仍存在而显示已登录。
  保留原 `has_auth` 恢复语义，通过 `has_owned_auth` 提供当前展示状态。
- 邮箱读取认证内的 email 或 JWT 展示字段；解码有大小限制，展示字段不用于认证或账号选择。
  只有对应配置的 ChatGPT access token 可查询订阅额度；其他已保存认证使用中性状态显示。
- 新增仅原生代码可用的凭据读取与固定 HTTPS `GET /backend-api/wham/usage` 查询，按需发送 Bearer 与
  可选 ChatGPT-Account-Id。读取凭据时短暂加锁，网络期间释放锁，返回前核对凭据摘要以防账号被替换。
  15 秒总超时、5 秒连接超时、禁止重定向、响应上限 1 MiB；错误不包含令牌、请求头或响应正文。
- 弹窗显示套餐、邮箱、剩余大字与进度条、已用比例、相对重置时间和本地完整日期；附加模型及代码审查
  额度默认折叠。窗口标题依据 `limit_window_seconds`，不固定把 primary 当作 5 小时；缺失值不补零。
  已到重置时间只提示刷新，不擅自恢复额度。保留同一账号上次成功结果时明确标记刷新失败。
- 新请求独立加载；关闭、切换账号或配置目录会使旧回调失效，列表没有后台额度请求。
- 认证缓存行为参考 [OpenAI 认证说明](https://learn.chatgpt.com/docs/auth)，额度周期说明参考
  [OpenAI 用量说明](https://learn.chatgpt.com/docs/pricing)；接口字段依据用户提供的响应样例及只读请求验证。
- 验证：Rust 全量 382 项测试通过，覆盖认证隔离、当前注销、JWT 展示、Pro/Team 窗口、空字段、请求头、
  错配与错误处理；参考 GET 返回 HTTP 200。浏览器使用合成账号验证同排徽标、双窗/单周/额外额度、
  未知值、失败重试、保留旧结果与关闭后的迟到响应；浅深色布局通过检查。
- TypeScript 检查、5 项现有前端回归、renderer 与 macOS App 构建及签名严格校验通过。
  已备份并替换本地测试 App，安装后可执行文件摘要与构建一致；此批改动记录在 Unreleased。

## 2026-09-08：子代理用量归属主会话

- 根因：用量聚合使用物理线程 ID 作为会话 ID，未接入会话管理的子代理分类与父子关系，导致内部线程
  出现在最近会话并增加活跃会话数。
- 复用 `sqlite_subagent_thread_ids` 与日志 `source` 判定，读取 `thread_spawn_edges` 和来源中的父线程。
  当前数据库的显式分类优先；旧库无来源字段、NULL 或空白不视为明确的普通用户会话，保留日志分类。
  所有身份读取保持只读及短锁等待；父关系冲突不随意选择归属。
- 每个物理线程继续独立计算精确用量、累计增量和重放去重，再递归汇入主会话。总量、日期和模型计数
  按主会话 ID 去重，最近列表在归并之后截取 10 条。筛选只命中子代理时也显示所属主会话的标题。
- 子代理归属与历史继承分别处理。用户主动 fork 保持独立主会话；纯 spawn 不自动视为继承历史，
  避免与父线程数值相同的独立请求被误删。只有明确 fork 或不同 ID 的继承元数据才检查重放前缀。
- 无法确定主会话的子代理不生成独立会话行，其可核实的 Token 仍进入全局、日期与模型统计，并说明范围。
  明确继承但父日志缺失时，继续保守跳过无法去重的历史并显示完整性提示。
- 验证：Rust 全量 363 项测试通过，覆盖多层子代理、同计数独立请求、归档副本、普通 fork、仅子代理
  命中筛选、SQLite-only 父子边、缺失来源字段、未知父关系与环路；TypeScript 检查通过。
- macOS 发布构建、renderer 构建及签名严格校验通过；备份旧版后替换并启动本地 App，安装摘要与构建一致。

## 2026-09-08：设置标签过渡与最近会话标题

- 设置的两类标签内容复用 `PageTransition`，保留旧内容完成淡出后再淡入新内容。用量组件保持实例和筛选，
  页面切换时通过活动状态停止接收旧请求，首次打开仍按需加载。
- 最近会话限制为 10 条，统计总量、趋势和模型分布仍覆盖全部匹配记录。
- 最近会话在截取列表后，按 ID 只读查询会话管理同源的 SQLite 标题。共用标题清理与优先级：标题、
  首条用户消息、预览文本；当前数据库优先，无聊天标题时以项目名兜底。数据库缺失或暂不可读时，
  继续尝试日志元数据中的项目名，最后显示“未命名会话”，不再把截短的会话 ID 当标题。
- 标题每次读取时重新查询，聊天改名后不需要重新解析 Token 日志；悬停可查看完整标题。
- 验证：Rust 全量 351 项测试通过，覆盖只读标题查询、当前/旧数据库优先级、项目名兜底、锁定数据库、
  标题改名与缓存并存、10 条展示上限不截断总量。TypeScript 检查及 renderer 生产构建通过。
  浏览器实际观察设置标签的 `exit`、`enter` 状态，切回保留日期/模型筛选，最近会话表为 10 行。
- macOS 发布构建与本地签名严格校验通过；备份旧版后替换本地 App 并启动，可执行文件摘要与本轮构建一致。

## 2026-09-08：直接复制、1M 上下文与用量统计

### 行为与实现

- 官方登录与第三方供应商点击复制后立即保存并留在列表，不打开编辑或确认框。名称由后端在锁内判重；
  官方副本按 Unicode 字符截断主体，为副本及数字后缀预留空间，避免合法长名称无法复制。
- 第三方复制在一个 SQLite 事务中完成检测项持久化、原启用项识别和副本保存，复制失败全部回滚；
  不写 live 配置或认证。前端保存列表使用请求序号，使迟到的旧列表读取不能覆盖新副本。
- 两类配置的 TOML 标题旁增加 1M 复选框。使用 `toml_edit` 修改草稿的顶层
  `model_context_window = 1000000`，未设置自动压缩时补 `model_auto_compact_token_limit = 900000`。
  已有自定义阈值、注释、MCP 及其他表均保留；取消时移除预设值或恢复本次编辑前的值。
  手工编辑 TOML 会同步复选框，格式错误时阻止开关覆盖草稿。新建官方配置先读取完整官方模板，认证仍为空。
- 设置分为通用设置和用量统计。统计页首次打开才读取本地 `sessions` 与 `archived_sessions` JSONL，
  命令在阻塞工作线程运行，使用独立缓存锁与前端加载状态，不占用供应商的操作锁。
- 统计优先使用精确 `last_token_usage`，缺失时以累计计数的增量计算；按会话身份合并归档与恢复片段，
  对累计快照、限额来源重复和分叉继承前缀去重。父日志不完整时暂缓对应分叉统计并明确提示。
  输入包含缓存，输出包含推理，两者不额外加到总量；没有历史记录的用量不推算。
- 按本地日历日提供今天、近 7 天、近 30 天和全部范围及模型过滤；展示总量、每日堆叠图、模型分布、
  最近 30 个会话和精确每日明细。最近会话显示上限不影响总量。读取失败可保留同一筛选的旧结果，
  空目录显示空状态，损坏、缺失和扫描限额通过覆盖提示说明。
- 内存缓存只保留身份、时间、模型和计数，不保存会话正文；支持追加读取、部分尾行重试、文件替换与删除。
  每次扫描有文件数、目录深度和字节预算，跳过符号链接；手动刷新重新核对完整文件内容。
  超出预算时跨次刷新继续当前轮扫描；复核尚未完成的完整缓存保留至新结果读完，避免中途清空已有用量。
- 参考 CC Switch 的配置与会话统计实现，参考版本为
  `farion1231/cc-switch@f3b18df12007d0fd79fd8ad8d310880664015197`。

### 验证

- Rust 全量 341 项测试通过；补充缺损提示与大库刷新后，统计模块最终 30 项全部通过（当前共 345 项）。
- TypeScript 类型检查、renderer 生产构建与 5 项现有前端供应商测试通过；Rust 格式检查与差异空白检查通过。
- Rust 开发检查无告警，macOS App 发布构建与本地签名严格校验通过；备份旧 App 后完成本地替换并启动，
  安装后的可执行文件摘要与本轮构建一致，版本号保持 0.3.15。
- 浏览器通过纯内存 IPC fixture 验证官方/API 直接复制、检测项仍为当前项、重复名称编号、两类 1M 保存，
  以及通用/用量标签、四种时间范围、模型筛选、每日明细、失败保留旧值、空状态及部分数据提示。
  浅色和深色截图布局正常，1280px 视口下页面无横向溢出。新建官方账号启用 1M、保存再打开时完整模板保留、
  认证为空，关闭预设后其他设置保留。所有测试均使用虚构账号或临时目录。

## 2026-09-08：多个官方登录配置与标题配色

### 设计与兼容

- 供应商新增入口可选择第三方 API 或官方 Codex 登录。默认官方配置和命名配置均可编辑名称、
  复制；新建与复制仅加入列表，点击启用后切换。空认证配置启用后由用户在 Codex 中完成登录。
- 官方配置使用稳定 ID，与可编辑名称分离。SQLite schema v4 按 CODEX_HOME 保存名称与选择状态，
  私有快照按目录和配置 ID 隔离完整 TOML 与认证；默认项继续兼容原快照位置。
- 当前官方配置拥有随后发生的登录与凭据刷新。切走前捕获到所属配置，未修改认证的保存使用最新值，
  显式清空则保留待登录状态。存在命名账号后，不再把来源无法归属的代理认证或旧历史备份混入默认账号。
- 修改 live 文件前校验读取时的配置与认证是否仍一致，写入、快照及选择记录失败时条件回滚。
  删除当前命名账号先切回默认项；默认项保留，损坏的命名快照仍可在列表中删除。
- 启动列表只读取元数据、当前认证与显式快照，不扫描历史或生成编辑草稿；显式编辑和恢复保留旧兼容能力。
  前端忽略过期列表及离开页面后迟到的复制编辑回调，避免覆盖新列表或留下空编辑草稿。
- 提示词顶部强调色复用页面蓝色变量，浅色为 `#2563eb`，深色为 `#76a8ff`。

### 验证

- 最终 Rust 全量 302 项测试通过，覆盖账号命名、完整复制、认证隔离、刷新、第三方往返、待登录归属、
  默认入口兼容、删除、数据库失败回滚、并发修改与启动不扫历史。
- TypeScript 类型检查、renderer 生产构建及 5 项前端供应商测试通过。
- Rust 开发构建检查无告警通过，格式化与差异空白检查通过。
- 浏览器使用隔离 IPC fixture 验证新增类型、默认改名、两账号往返、复制及独立清空认证、刷新后保留、
  删除当前账号回到默认项；实际 DOM 确认两种主题的提示词标题蓝色。
- 界面验证使用虚构账号，文件与数据库持久化由临时目录中的 Rust 回归验证，未操作真实用户认证。

## 2026-09-08：模板同步与页面操作隔离、保留首次检测的供应商

### 根因与修复

- 后台模板同步本身仅设置 `promptSyncing`，但远程下载与模板内容读取共用长时间持有的互斥锁。
  同步期间查看/编辑或启用模板会等待该锁，前端共享的 `loading` 又会使供应商页面的按钮禁用。
  查看与启用现在依次使用本地修改、已提交的 SQLite 缓存和打包模板；联网更新统一由同步命令处理。
- 模板详情请求使用页面独立的加载状态。离开提示词页、切换配置目录、打开另一编辑对象时使旧请求
  失效，迟到的成功、失败及清理回调不能覆盖新的编辑草稿或其他页面的状态。
- 本地编辑允许与同步并行。同步清理在数据库事务中保留已有本地修改的模板，返回目录前再次读取
  最新修改记录，避免下载开始时的旧快照误删刚编辑过的下架模板。
- 首次显示的 CC Switch 供应商可能只是当前 Codex live 配置的检测结果，并未执行数据库导入。
  旧逻辑在切换前只更新已有的唯一匹配记录，因此未保存的检测项会在 live 配置被替换后消失。
  现在明确启用/切换时保存未匹配的当前第三方配置：在同一 SQLite 事务内完成匹配、分配不冲突的
  记录 ID 和保存，保留完整 TOML 与独立 API Key，切换失败时沿用条件回滚。多个匹配项不擅自覆盖。
- 当前供应商标记只接受与实际 live 配置匹配的已保存 ID；临时或冲突 ID 不覆盖后端识别结果。
  普通状态读取继续保持无配置写入，不触发 CC Switch 全量导入。

### 验证

- Rust 回归覆盖同步锁持有期间读取本地模板、同步期间新增本地修改、首次供应商启用/直接切走、
  官方与第三方往返、完整模板及密钥保留、ID 冲突、多份相同配置和失败回滚。
- 使用仅模拟 Tauri IPC 的浏览器测试页面复现旧版按钮禁用；修复后同步与详情请求同时挂起时，
  可切换第三方或官方、打开供应商新增表单。迟到详情不覆盖新草稿，同步失败后可重试。
- 隔离界面复现入口为 `node apps/desktop/tests/serve-ui-fixture.mjs`，步骤见
  `apps/desktop/tests/fixtures/README.md`；其中供应商持久化为模拟响应，实际保证来自 Rust 回归测试。

## 2026-08-11：供应商认证往返、模板完整性与启动读取

### 决策

- 新建和 CC Switch 导入的供应商保存完整、无密钥的 TOML 模板；API Key 单独保存在供应商
  数据库字段中。完整模板是该供应商的权威配置，切换时恢复项目、插件、MCP、桌面、功能和
  环境设置；旧版稀疏模板仍以当前 live 配置为底稿兼容合并。
- 官方 OAuth 只保存在 Codex-X 的独立可信快照和官方 live `auth.json` 中。切换第三方时，
  live `auth.json` 改写为只含 `OPENAI_API_KEY` 的第三方认证；第三方 TOML 不保留
  `auth_mode` 或 `experimental_bearer_token`，切回官方时再恢复完整官方认证。
- 官方配置独立保存完整 `config.toml`、模型和 OAuth/API Key `auth.json`。官方与第三方往返
  按目标方向写入：第三方凭据先于第三方路由，官方路由先于官方凭据；失败时只回滚已经写入
  的第一步。未登录的官方 Reset 快照也必须保留完整 TOML。自动捕获不得让第三方 API Key
  覆盖可信 OAuth 快照。
- 兼容无 `auth_mode` 但包含有效 token、且不含 API Key 的旧版官方 OAuth；损坏的 live
  `auth.json` 与损坏的应用内官方快照都不阻塞状态读取或供应商切换，且绝不能被晋升为官方
  认证快照。完整官方 TOML 是模型字段的权威源，独立模型输入只补齐缺失值。
- 启动状态读取不得迁移提示词、修改文件权限、捕获认证或扫描历史备份；概览在结果返回前显示
  “正在读取”，失败后显示“读取失败”。
- Codex-X 内部数据库使用持久 `user_version`。已升级数据库的普通打开只读版本号；迁移、历史
  清理和版本更新在同一个 `BEGIN IMMEDIATE` 事务完成，失败可重试，同路径替换后可重新初始化。
- Codex 版本探测在 blocking worker 中执行，固定候选先探测，目录候选流式探测，所有阶段共享
  同一个总 deadline，避免 Windows 慢盘或重定向用户目录拖住启动。
- 对照 CC Switch `076c2744ceb622b85771bff57668d43ed70809f8` 的完整 provider config 和
  默认 `preserveCodexOfficialAuthOnSwitch = false` 路径：第三方认证直接写入 `auth.json`。
  `95f2dd41262f01209100128ea647dbd054b5624a` 的 OAuth + provider-scoped bearer 行为仅作为
  兼容策略参考，不再作为 Codex-X 默认切换语义；
  Codex-X 额外保留官方独立快照、live 配置并发检查与条件原子回滚。本节决策替代 2026-07-29
  中关于第三方 bearer token 和模板激活的旧约束。

## 2026-07-30：live 配置并发与失败回滚

### 已确认根因

Codex-X 的供应商、提示词、备份恢复和状态刷新曾各自读写 `config.toml`。即使每次
写出的内容都是合法 TOML，只要 Codex、CC Switch 或另一条 Codex-X 操作在“读取旧值”和
“整文件替换”之间更新配置，后写入者就会覆盖前一个新值。多文件操作在中途失败时还可能
留下 config、auth、AGENTS 或应用数据库互不对应的混合状态。

### 约束

- 所有 Codex live 配置写入共用跨进程文件锁；已经持锁的内部函数使用 `_locked` 入口，
  禁止重复获取非重入锁。
- 写入前保存原始字节快照，原子替换前再次核对当前内容；快照已过期时拒绝写入并要求刷新。
- 回滚只能覆盖本次操作实际写出的期望值。多文件回滚先统一预检，再逆序恢复，不能覆盖
  Codex 或 CC Switch 在失败后写入的新内容。
- 活动供应商编辑从身份识别到数据库更新、live 热更新和最终状态构造都处在同一配置锁
  边界内。
- 备份恢复只接受备份根目录下的单个普通目录名和普通文件；元数据 ID、`CODEX_HOME` 与
  `had_config` / `had_auth` / `had_agents` 声明必须和实际载荷完全一致。损坏或缺失元数据、
  跨目录恢复、路径穿越、额外/缺失载荷和符号链接均在 live 写入前拒绝。旧提示词结构先在
  内存迁移，再作为一次可回滚写入落盘。
- 状态刷新在同一锁内完成旧配置迁移、官方认证捕获和状态构造；认证快照失败必须返回错误，
  不得吞掉后显示成功状态。

## 2026-07-30：关闭主窗口后驻留系统托盘

### 问题

Codex-X 原先没有系统托盘和窗口关闭事件处理。用户点击主窗口关闭按钮后，
Tauri 事件循环随最后一个窗口关闭而结束，后台管理能力也随之退出。

首版修复只调用了 `window.hide()`。这可以保留进程和窗口状态，但 macOS 应用仍是
`ActivationPolicy::Regular`，因此 Dock 图标不会消失，尚未真正进入仅菜单栏驻留状态。

### 决策

- 仅拦截标签为 `main` 的主窗口关闭请求，将窗口隐藏而不是销毁或结束进程。
- macOS 关闭时同时隐藏 Dock 并切换为 `ActivationPolicy::Accessory`；恢复窗口时切回
  `Regular`。这两项都执行，避免不同 macOS 版本残留 Dock 图标。
- Windows 关闭时显式启用 `skip_taskbar`，恢复时关闭，避免隐藏窗口仍占用任务栏。
- macOS 左键点击顶部栏图标打开菜单；Windows 左键点击托盘图标直接恢复窗口。
- 选择“显示 Codex-X”会恢复、取消最小化并聚焦原主窗口。
- 托盘菜单始终提供“退出 Codex-X”，用于明确结束后台进程。
- macOS 再次点击 Dock 图标时也恢复已经隐藏的主窗口。
- 不拦截应用级退出或更新器的显式重启，保证退出菜单、`Cmd+Q` 和安装更新仍可结束进程。

### 参考实现

对照 CC Switch `56fb46c09310ff52dabefd2b32f0e799e8357d9e` 的
`src-tauri/src/lib.rs` 和 `src-tauri/src/tray.rs`：其 Windows 路径使用
`set_skip_taskbar`，macOS 路径同时使用 `set_dock_visibility` 和
`set_activation_policy`。Codex-X 复用相同的平台生命周期规则，但保留自己的简化托盘菜单。

### 验证

- `cargo fmt --all -- --check`、`cargo check --locked --lib` 和 `git diff --check` 通过。
- `cargo test --locked --features windows-runtime-check` 全量通过。
- `pnpm --dir apps/desktop typecheck` 和 `pnpm --dir apps/desktop build:renderer` 通过。
- macOS 本机包实测：窗口显示时 Launch Services 类型为 `Foreground`；点击关闭按钮后
  主窗口消失、PID 保持不变，类型切换为 `UIElement`。这证明应用已从 Dock 模式进入仅
  顶部栏驻留模式；再次激活后同一 PID 恢复为 `Foreground`，配置数据正常加载。
- `Cmd+H` 后可以恢复同一进程；`Cmd+Q` 和托盘“退出 Codex-X”仍用于真正结束进程。
- 本机 Windows 交叉编译在第三方依赖 `ring` 阶段因缺少 MSVC C 头文件停止，尚未进入
  项目代码；发布前仍须以 GitHub Windows Actions 的原生构建结果为准。

## 2026-07-29：供应商切换与配置完整性

### 已确认根因

旧实现会把检测到的整份历史 `config.toml` 保存到供应商记录，并在后续切换时覆盖
当前文件。该文件仍可能是合法 TOML，但新增加的项目、插件、MCP 和功能设置会丢失，
表现为 Codex 要求重新设置或配置被“破坏”。

### 约束

- 供应商记录可以保存完整 TOML，作为导入与编辑模板；其中的密钥必须剥离并由独立字段保存。
- 激活供应商必须基于最新 live 文档，仅合并目标 provider 与 model 字段，不回放模板中的
  项目、插件、MCP 或其他通用设置。
- 官方认证快照与中转认证严格隔离，供应商切换不能覆盖可信官方认证。
- 自动捕获官方认证只接受明确的 ChatGPT 登录模式及 access/refresh/id token；代理路由下的
  API key 属于不可信来源，不能晋升为官方快照。
- 用户明确从已确认的官方 live 路由切换到第三方时，允许保存非空的官方 API Key 快照，
  以保证“官方 API Key → 第三方 → 官方”往返不会丢失认证；只读状态刷新仍不自动采信 API Key。
- 第三方密钥只写入活动 provider 表的 `experimental_bearer_token`，不写入 `auth.json`。
- 官方与第三方路由都使用稳定的 `custom` 会话分桶；官方 provider 不设置 `base_url`，继续
  使用真实 OpenAI 后端和独立保存的官方 `auth.json`。
- 编辑活动供应商必须原子更新原记录，不能通过“保存后再切换”制造副本。
- 切换只影响后续新建会话，不强制重启 Codex 客户端。

## 2026-07-29：会话供应商同步

### 决策

- 统一使用 Codex 可识别的 `custom` 共享分桶。
- 活动会话的唯一权威来源是当前存储根目录中最高版本的可读 `state_N.sqlite`；旧 SQLite、
  单独存在的 JSONL 和 JSONL 行数都不能当作会话数量。
- 扫描 JSONL 前先按活动 thread ID 和 SQLite 的 `rollout_path` 建立候选集。标准命名孤立
  文件和未被引用的非常规文件不读取、不计入活动 rollout，也不会因损坏而阻断同步。
- SQLite 一旦为线程提供 `rollout_path`，该路径就是唯一权威文件；不得再按 UUID 文件名
  回退并修改同 ID 的旧副本。权威路径缺失、不可读或越界时必须标记扫描失败并阻止同步。
- `rollout_path` 指向的文件必须包含与该 SQLite 记录相同的 `session_meta.id`；缺少元数据、
  ID 串线或多个线程引用同一个文件时必须阻止同步，不能只更新 SQLite 后报告成功。
- SQLite 明确引用的非常规 rollout 仅在 `sessions` / `archived_sessions` 内、且为非符号链接
  普通 JSONL 时处理；活动候选无法读取或解析时保持失败关闭，不能显示“已同步”。
- 只修改 rollout 元数据和活动 SQLite 线程索引中的 provider 字段。
- 不修改会话正文、工作目录、用户事件标记或全局界面状态。
- 备份和回滚只覆盖本次实际修改的会话存储。

### 来源说明

当前 `b-nnett/codex-plusplus` 源码没有 rollout JSONL/SQLite 分桶同步实现，不能把这部分
描述为对其逐行复刻；`custom` 共享分桶语义来自对 CC Switch 可验证实现和本地 Codex
存储行为的交叉检查。
